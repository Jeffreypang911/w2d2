

<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>

<ul>
    <% for(var value in urls) { %>
    <% if(value === shorturl) { %>
    <li> <%= urls[value] %></li>
    <li> <%= value %></li>
    <% }; %>
    <% }; %>
</ul>
</body>
</html>